"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Maps the values of an array to an object using a function.
 *
 * @param {[]} arr
 * @param {Function} fn
 */
var mapObject = function (arr, fn) {
    return arr.reduce(function (acc, el, i) {
        acc[el] = fn(el, i, arr);
        return acc;
    }, {});
};
exports.default = mapObject;
