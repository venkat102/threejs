"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var kyouka_1 = __importDefault(require("./kyouka"));
/**
 * Preview an Image when user uploads an image file.
 *
 * @param {any} inputEl
 * @param {any} imgEl
 * @param {Function} cb
 */
var previewImage = function (inputEl, imgEl, cb) {
    inputEl.addEventListener("change", function (e) {
        var files = e.target.files;
        var file = files ? files[0] : null;
        kyouka_1.default.loadImageAsBase64URL(file, function (base64URL) {
            if (base64URL) {
                imgEl.src = base64URL;
                cb(base64URL);
            }
        });
    });
};
exports.default = previewImage;
