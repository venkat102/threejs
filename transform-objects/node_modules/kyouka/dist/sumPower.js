"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Calculates the sum of the powers of all the numbers from start to end (both inclusive).
 * @param end
 * @param power
 * @param start
 */
var sumPower = function (end, power, start) {
    if (power === void 0) { power = 2; }
    if (start === void 0) { start = 1; }
    return Array(end + 1 - start)
        .fill(0)
        .map(function (x, i) { return Math.pow((i + start), power); })
        .reduce(function (a, b) { return a + b; }, 0);
};
exports.default = sumPower;
